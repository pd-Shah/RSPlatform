# by pedram shahsafi <pd.shahsafi@gmail.com>
image: docker

services:
  - docker:dind

stages:
  - build
  - test
  - deploy

before_script:
  - echo "before script running..."

APIProviderTest:
  stage: build
  script:
    - pip install docker-compose
    - docker-compose build
    - docker-compose up -d

  stage: test
  script:
    - echo "APIProvider test stage is running..."

  stage: deploy
  script:
     - echo "APIProvider deploy stage is running..."
     - echo "nothing to do right now..."
